#include<iostream>

int m, n;

int dp[m][n];


using namespace std;

int main(){
  int m, n;
  cin >> m >> n;
  int dp[m][n];
  memset(dp, 0, sizeof(dp));
  for (int i = 0; i < ((1 << n)-1); i++){
    for (int j = 0; j < ((1 << n) - 1); j++){

    }
  }
  return 0;
}
