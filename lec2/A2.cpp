#include <iostream>
#include <vector>

using namespace std;



int main() {
    int n, k;
    vector< vector<int> > gr(n);
    for (int i = 0; i < n; i++){
        for (int j = 0; j < n; j++){
            gr[i][j] = 0;
        }
    }
    
}