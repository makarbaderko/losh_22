#include <iostream>
#include <vector>
#include <cstdio>
#include <string>
#include <fstream>

using namespace std;


int main(){
    freopen("ds.in", "r", stdin);
    //freopen("ds.out", "w", stdout);
    vector<int> v();
    string x;
    while (getline(stdin, x)) {
        cout << x << endl;
    }
}