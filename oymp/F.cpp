#include <iostream>
#include <vector>

using namespace std;

int main() {
  int n
  vector<pair<int, int> > set();
  cin >> n;
  for (int i = 0; i < n; i++){
    int x, y;
    cin >> x >>y;
    set.push_back(make_pair(x, y));
    int a, b;
    max_element(pairs.begin(),
            pairs.end()
    for (int x = 0; x <=)
  }
}
